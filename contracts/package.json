{
  "name": "cifi-migration-contracts",
  "version": "1.0.0",
  "description": "CIFI Migration Smart Contracts",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:xdc": "hardhat run scripts/deploy.js --network xdc",
    "deploy:testnet": "hardhat run scripts/deploy.js --network xdcTestnet",
    "verify": "hardhat verify --network xdc"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1"
  }
}

